services:
  prometheus-iot:
    image: prometheus
    ports:
      - "9090:9090"
    restart: unless-stopped

  grafana:
    image: grafana
    container_name: grafana
    ports:
      - "3000:3000"
    restart: unless-stopped